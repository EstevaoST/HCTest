using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class GameData
{
    public enum GameScenes
    {
        MainMenu = 0, GameScene = 1, VictoryScreen = 2, GameOverScreen = 3, AdScreen = 4
    }
}
